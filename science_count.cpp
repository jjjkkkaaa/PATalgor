#include <iostream>
#include <string>
using namespace std;
int main(int argc, char const *argv[]) {
  string s;
  cin >> s;
  string flag = (s[0] == '+' ? "" : "-");
  int pos = s.find('.');
  return 0;
}
